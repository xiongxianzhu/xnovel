# xnovel 实施任务

状态使用 `[ ]`、`[x]` 或 `[!]`。`[!]` 表示存在阻塞，并在任务下说明原因。

## Phase 0：仓库基础

- [x] T-001 建立 Monorepo 顶层目录。
- [x] T-002 建立 FastAPI 工程骨架和健康检查。
- [x] T-003 补充产品、架构、数据库、技术、API、AI 和部署文档。
- [x] T-004 建立 Vite + React + TypeScript Web 工程。
- [x] T-005 增加根目录 `.gitignore` 和项目 README。
- [x] T-006 建立贡献指南、Issue/PR 模板与 API/Web/Desktop 持续集成。

退出条件：全新检出后可以分别启动 API、Web 和 Desktop，文档链接有效，Pull Request 自动执行三端质量检查。

## Phase 1：领域契约

- [x] T-101 定义作品、文档、人物和世界设定的数据模型（见 [`database.md`](database.md)）。
- [x] T-102 确定 Web 本地账号、服务端会话和作品访问边界（见 [`database.md`](database.md)）。
- [x] T-103 定义首版正文保存、并发冲突和版本策略（见 [`database.md`](database.md)；版本保留周期仍按 PRD 后续决策处理）。
- [x] T-104 确定首批 AI Provider、密钥归属和用量限制（见 [`ai-integration.md`](ai-integration.md)）。
- [x] T-105 根据 FastAPI OpenAPI 生成前端 API 客户端。
- [x] T-106 实现动态注册开关、首个管理员命令和管理员审计。
- [x] T-107 实现真实登录会话、用户资料、头像与 Web 全局 Logo 存储；邮箱与手机号验证延后。
- [x] T-108 实现 Web 登录闭环、`zh-CN`、`zh-TW`、`en-US`、五套主题和用户偏好设置。
- [x] T-109 实现管理员 CLI 初始化、可选联系方式与首次强制改密。
- [x] T-110 建立 Web 控制台壳层、角色菜单与管理员占位入口。

退出条件：P0 领域拥有真实 Schema、API 路径、错误行为和验收标准。

## Phase 2：首个纵向切片

- [x] T-201 创建和打开作品。
- [x] T-202 建立可排序的作品文档树。
- [x] T-203 编辑并保存单文档短篇。
- [x] T-204 覆盖加载、空状态、保存失败和冲突恢复。
- [x] T-205 增加 API、组件和关键流程测试。

退出条件：用户可以完成“创建作品—写作—保存—重新打开”闭环。

## Phase 3：规划与设定

- [x] T-301 增加大纲管理。
- [x] T-302 增加人物资料。
- [x] T-303 增加世界设定。
- [x] T-304 建立正文与设定的引用边界。
- [x] T-305 增加 Markdown 或纯文本导出。

## Phase 4：AI 辅助

- [x] T-401 建立 Provider 接口和模型配置。
- [x] T-402 建立上下文构建器和最小上下文策略。
- [x] T-403 实现可取消的 AI 任务和流式事件。
- [x] T-404 实现候选结果确认，不直接覆盖正文。
- [x] T-405 增加用量、错误和审计元数据。
- [x] T-406 实现 Web 私有 Skill 上传、校验、不可变版本和所有权隔离。
- [x] T-407 实现 Skill 列表、详情、`SKILL.md` 编辑、删除、启用和禁用。
- [x] T-408 将明确选择的 Skill 作为不可信上下文，并保存无外键任务快照。
- [x] T-409 实现管理员 Skill 非内容元数据、隔离/解除隔离和审计边界，管理员普通接口不得读取 Skill 内容。

## Phase 5：桌面端与发布

- [x] T-501 建立 Electron 主进程和安全 preload。
- [x] T-502 建立本地 SQLite Schema、迁移、事务和升级前备份。
- [x] T-503 复用 React 应用，并通过平台存储适配器隔离 FastAPI 与 SQLite。
- [x] T-504 使用 `safeStorage` 加解密并由主进程持久化 AI 凭据密文，再从主进程调用 Provider。
- [x] T-505 配置桌面安装包和自动更新，确保更新不删除用户数据。
- [x] T-506 建立 Windows x64、macOS x64 / arm64 的 CI 与发布流程。
- [x] T-507 验证离线写作、数据库升级、备份恢复和故障版本回滚。
- [x] T-508 实现五套跨端主题及 `system`、`light`、`dark` 模式。
- [x] T-509 只读扫描 `~/.agents/skills/` 一级目录，并实现列表、详情、重新扫描、启用和禁用。
- [x] T-510 在每次 Desktop AI 任务前复核 Skill 指纹与资源限制，且不暴露 Skill 写入 IPC。

退出条件：无需登录和网络即可完成本地写作闭环；renderer 无法直接访问 SQLite、文件系统或 AI 密钥；上一稳定版数据库可以安全升级和恢复。
